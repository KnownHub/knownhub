apply plugin: 'java'

sourceCompatibility = 1.8

archivesBaseName = rootProject.name + "-edge-api"

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {

    testCompile group: 'junit', name: 'junit', version: "${versions.junit}"
}